@model MhrsWeb.Areas.AdminPanel.Models.RandevuSaatiViewModel

@{
    ViewBag.Title = "UpdateTarihi";
    Layout = "~/Areas/AdminPanel/Views/Shared/_Layout.cshtml";
}

<form action="@Url.Action("TransferPostMethod","RandevuSaati")" method="POST">

    <div class="form-horizontal">
        <h4>Doktor Saatlerini Güncelle > @Model.doktor.FullName</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        <div class="from group">
            @Html.Label("Çalıştığı Tarihler")
            <select name="RandevuTarihi" id="RandevuTarihi" class="form-control">
                @foreach (var item in Model.tarihListesi15)
                {
                    <option selected value="@item.ToShortDateString()">@item.ToShortDateString()</option>
                }
            </select>
        </div>
        <hr />

        <input type="hidden" id="@Model.doktor.Id" name="Id" value="@Model.doktor.Id" />


        <div class="form-group">
            <a href="@Url.Action("Index","RandevuSaati")" class="btn btn-default"><< Geri Dön</a>
            <input type="submit" value="Saati Güncelle" class="btn btn-primary" />
        </div>
    </div>

</form>
